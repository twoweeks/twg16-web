const config = {
    paths: {
        send: 'send',
        view: 'view',
    },

    host: 'http://localhost:1337/request.php',

    api_keys: {
        recaptcha: '',
    },
};

export default config;
