const config = {
    paths: {
        send: 'send',
        view: 'view',
    },

    links: {
        rules: 'https://16.twg.host/p/rules.html',
    },

    host: 'http://localhost:1337/request.php',

    api_keys: {
        recaptcha: '',
    },
};

export default config;
